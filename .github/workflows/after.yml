name: Second Workflow

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  second-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Second workflow started because the first workflow finished"
